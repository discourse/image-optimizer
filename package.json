{
  "scripts": {
    "build": "bash ./build.bash",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@saschazar/wasm-image-loader": "^4.0.0",
    "@saschazar/wasm-mozjpeg": "^3.0.0"
  }
}
